name: movil
description: "App Cliente AUXIA - Asistente Vehicular"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: ^2.2.2
  image_picker: ^1.0.7
  geolocator: ^11.0.0
  permission_handler: ^11.3.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  jose: ^0.3.0
  intl: ^0.19.0
  record: ^6.0.0
  web_socket_channel: ^2.4.0
  speech_to_text: ^7.1.0
  flutter_local_notifications: ^18.0.0
  firebase_core: ^2.24.0
  firebase_messaging: ^14.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true